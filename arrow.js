const doubleIt = num =>num * 2;
const add=(x,y)=>x+y;

const doMath = (x,y) =>{
    const sum=x + y;
    const diff = x - y;
    const result = sum * diff;
    return result;
}

const result = add(50,60);
const result2=doMath(7,5);
console.log(result);
console.log(result2); 
